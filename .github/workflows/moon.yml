name: Moon

on:
  workflow_dispatch:  # Déclenchement manuel

jobs:
  fetch-moon:
    runs-on: ubuntu-latest

    steps:
      - name: Get Moon weather
        uses: alesil/actions-curl@v1.2
        with:
          curl: wttr.in/Moon
          
